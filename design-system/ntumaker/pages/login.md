# 登入頁規範

登入頁在同一張紙張式面板中提供「社員」與「管理員」兩個分頁，不將兩種驗證方式混在同一個表單。

- 社員分頁只顯示 Email，說明系統會核對資料庫帳號狀態與社員期限，通過後直接登入。
- 管理員分頁顯示 Email、密碼及顯示／隱藏密碼控制，並說明登入後可管理的內容。
- 分頁使用 `role="tablist"`、`role="tab"` 與 `role="tabpanel"` 表達結構，選取狀態不能只靠顏色。
- 欄位必須有可見標籤、瀏覽器自動填寫屬性與靠近欄位的錯誤回饋。
- 管理員與社員成功登入後分別導向 `/admin`、`/learn`。
